import { PlaceholderPage } from '@/components/PlaceholderPage'

export default function ProjectsPage() {
  return (
    <PlaceholderPage
      eyebrow="Placeholder"
      title="Projects"
      description="A future home for project lists, progress snapshots, and navigation into project detail views."
      meta="The `/projects/:projectId` route is already wired in, so a project list can link directly into richer views later."
    />
  )
}
