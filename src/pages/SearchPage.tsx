import { PlaceholderPage } from '@/components/PlaceholderPage'

export default function SearchPage() {
  return (
    <PlaceholderPage
      eyebrow="Placeholder"
      title="Search"
      description="A future search surface for projects, items, tags, and saved filters."
      meta="The page is ready to accept a command palette, search index, or filtered result list later."
    />
  )
}
