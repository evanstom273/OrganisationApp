import { PlaceholderPage } from '@/components/PlaceholderPage'

export default function InboxPage() {
  return (
    <PlaceholderPage
      eyebrow="Placeholder"
      title="Inbox"
      description="A future capture and triage area for incoming items."
      meta="This page is intentionally simple for now so the scaffold stays focused on routing, navigation, and settings."
    />
  )
}
